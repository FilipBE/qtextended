This is Russ Nelson's lighhtweight At daemon, with additions for Sharp (just
enabling the RTC wakeup in the APM BIOS).

Normally, "atd" would be run as a system service, and the "at" script
would be the standard Unix at(1) command.


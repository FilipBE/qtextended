This component provides an abstract interface for a call screen. The call screen 
is shown while calls are in progress and allows operations such as merging, putting 
calls on hold or hangup operations.
